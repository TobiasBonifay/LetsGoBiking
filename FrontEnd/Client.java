/**
 * Java client (SOAP)
 */
public class Client {
    public static void main(String[] args) {
        // Create a new instance of the client
        Client client = new Client();
        // Start the client
        client.start();
    }

    // Start the client
    public void start() {
    }
}